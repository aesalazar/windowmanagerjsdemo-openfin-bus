<h1>OpenFin Demo Application</h1>

This is the OpenFin application that will connect to the OpenFin runtime and pub/sub over the message bus. First start the web server to provide the html/javascript:

`npm start`

Then launch the OpenFin Client application via the CLI:

`npm run openfin`